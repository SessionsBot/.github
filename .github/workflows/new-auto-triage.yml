name: "New Contribution - Auto Triage"
run-name: Contribution by ${{github.actor}} - Auto Triage - ${{github.event_name}}

on:
  workflow_call:

permissions:
  pull-requests: write
  issues: write

jobs:
  TRIAGE-START:
    runs-on: ubuntu-latest
    steps:
      # STEP 1: Check if PR author is a member of the org
      - name: Check Org Membership
        id: check-membership
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const org = 'SessionsBot';

            let username;
            let issue_number;

            if (context.payload.pull_request) {
              username = context.payload.pull_request.user.login;
              issue_number = context.payload.pull_request.number;
            } else if (context.payload.issue) {
              username = context.payload.issue.user.login;
              issue_number = context.payload.issue.number;
            } else {
              throw new Error("This workflow only supports issues or pull requests.");
            }

            let isMember = false;

            try {
              await github.rest.orgs.checkMembershipForUser({
                org,
                username
              });
              isMember = true;
              console.log(`${username} is a member of ${org}`);
            } catch (error) {
              if (error.status === 404) {
                console.log(`${username} is NOT a member of ${org}`);
              } else {
                throw error;
              }
            }

            core.setOutput('is_member', isMember);

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              labels: [isMember ? 'Organization Contributor' : 'Outside Contributor', 'Needs Review']
            });


      # STEP 2: Only post comment if it's an outside contributor (optional)
      - name: Post Thanks Comment
        if: steps.check-membership.outputs.is_member == 'false' && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "👋 Thanks for the Pull Request! One of our code reviewers will check this out as soon as possible."
            })

