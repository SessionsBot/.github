name: Manual Label Sync - 🏷️

on:
  workflow_call:
    inputs:
      deleteUnmatched:
        type: boolean
        description: 'Delete labels not in config? (true or false)'
        required: true
        default: false

permissions:
  issues: write

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout - Config repository
        uses: actions/checkout@v4
        with:
          repository: SessionsBot/.github
          ref: main
          path: config-repo

      - name: Sync labels - Config file
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const path = require('path');
            const syncLabels = require('config-repo/.github/scripts/sync-labels.js');
            const deleteUnmatched = '${{ github.event.inputs.deleteUnmatched }}' === 'true';
            await syncLabels({ github, context, core }, deleteUnmatched);

      - name: Sync labels - Config file
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            console.log('✅ Yay! This job has completed successfully!')
