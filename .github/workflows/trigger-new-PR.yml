name: "New Pull Request"

on:
  pull_request:
    types: [opened, reopened, ready_for_review]

jobs:
  post-comment:
    uses: SessionsBot/.github/.github/workflows/reuseable-PR-Comment.yml@main
    permissions:
      pull-requests: write
      issues: write