name: "New Pull Request - Actions"

on:
  workflow_call:

permissions:
  pull-requests: write
  issues: write

jobs:
  reusable-comment:
    runs-on: ubuntu-latest
    steps:
      - name: Post Comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "👋 Thanks for the Pull Request! One of our code reviewers will check this out as soon as possible."
            })
      - name: Add Triage Label
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['Needs Review']
            })